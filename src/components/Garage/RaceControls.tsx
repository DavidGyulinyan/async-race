import React from 'react';

const RaceControls: React.FC = () => {
  return (
    <div className="race-controls">
      <button>Start Race</button>
      <button>Reset</button>
    </div>
  );
};

export default RaceControls;
